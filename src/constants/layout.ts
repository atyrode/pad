/**
 * Layout constants
 * Centralized values for consistent layout calculations
 */
export const LAYOUT_CONSTANTS = {
  // Game area dimensions as percentage of viewport (matching w-[95%] h-[95%] in page.tsx)
  GAME_AREA_WIDTH_PERCENTAGE: 0.95,
  GAME_AREA_HEIGHT_PERCENTAGE: 0.95,
} as const;

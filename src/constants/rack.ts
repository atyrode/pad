/**
 * Rack-related constants and types
 */

/**
 * Standard rack size for Scrabble-like games
 */
export const RACK_SIZE = 7;

/**
 * Type for rack indices (0 to RACK_SIZE-1)
 * Using a simple number type to avoid breaking changes
 */
export type RackIndex = number;

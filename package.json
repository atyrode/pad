{
  "name": "skrabble",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.13",
  "engines": {
    "bun": "1.3.13",
    "node": "24.x"
  },
  "scripts": {
    "dev": "bun run build:engine && next dev",
    "build": "bun run build:engine && next build",
    "build:engine": "bun scripts/build-engine.ts",
    "typecheck": "tsc --noEmit --incremental false",
    "lint": "eslint",
    "test": "bun test src",
    "test:rust": "cargo test --locked --workspace",
    "test:parity": "bun scripts/verify-engine.ts",
    "check": "bun run build:engine && cargo fmt --all -- --check && cargo clippy --locked --workspace --all-targets -- -D warnings && bun run typecheck && bun run lint && bun run test:rust && bun run test && bun run test:parity"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "lucide-react": "^0.548.0",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/bun": "1.3.13",
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "eslint": "^9.39.5",
    "eslint-config-next": "16.3.4",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3"
  }
}
